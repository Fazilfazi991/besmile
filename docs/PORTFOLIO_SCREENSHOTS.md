# Portfolio screenshot plan

Capture each screen at 1440 × 1000 after entering the local, backend-free demo. Use only the fictional `DEMO-` identifiers; do not crop in browser autofill or other personal data.

1. **Employee dashboard** — `/employee/dashboard`. Show the synthetic workload, tasks, and the Fusion Ventures Demo banner.
2. **Patient directory** — `/employee/patients`. Show several fictional patients with no real contact details.
3. **Patient profile** — `/employee/patients/demo-amara-vale`. Show a synthetic timeline and add a temporary note.
4. **Scheduling** — `/clinician/schedule`. Show the synthetic clinical workflow.
5. **Notifications** — `/employee/notifications`. Show synthetic in-app notifications only; do not enable browser push.
6. **Reports overview** — `/admin/reports`. Show local illustrative analytics; export and report generation are disabled.

Before capture, verify that the demo banner is visible, the URL is the demo deployment, and no user-supplied or production data appears in any card, browser autofill, notification, or page title.
