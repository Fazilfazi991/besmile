# Portfolio screenshot plan

Capture the Director-facing screens at 1440 × 1000 after entering the backend-free demo. Use only fictional `DEMO-` identifiers; do not capture browser autofill or personal data.

1. **Director executive overview** — `/admin`. Show the real Director dashboard shell, KPI cards, revenue/sales trend, finance overview, lead pipeline, reporting-period selector, and Fusion Ventures Demo banner.
2. **Client management** — `/admin/patients`. Show the 20-record synthetic client directory, search state, care focus, clinician assignment, progress, and status columns.
3. **Staff management** — `/admin/employees`. Show the synthetic employee directory with role, department, designation, joining date, and status filters. Keep the disabled Add employee action visible as a safe-action example.
4. **Client detail workflow** — `/admin/patients/demo-amara-vale`. Show the synthetic care timeline and temporary-note interaction.
5. **Portfolio reporting** — `/admin/reports`. Show illustrative local analytics; export and report generation remain disabled.

The current deployment is `https://besmile-public-demo.vercel.app`. Verify the demo banner and `noindex, nofollow` metadata before capture.

Before capture, verify that the demo banner is visible, the URL is the demo deployment, and no user-supplied or production data appears in any card, browser autofill, notification, or page title.
