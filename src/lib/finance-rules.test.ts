import {describe,expect,it} from 'vitest';import {canRecordInvoicePayment,financeAccountBalance,financeTotals,invoiceTotal,invoiceValidationMessage,outstandingInvoice,payrollNet} from './finance-rules';
describe('finance rules',()=>{it('excludes archived transactions and calculates profit',()=>{expect(financeTotals([{transaction_type:'income',amount:100},{transaction_type:'expense',amount:40},{transaction_type:'income',amount:25,archived_at:'x'}])).toEqual({income:100,expenses:40})});it('calculates account balances linearly across mixed ledger movements',()=>{expect(financeAccountBalance(0,[{transaction_type:'invoice_payment',amount:75},{transaction_type:'invoice_payment',amount:125},{transaction_type:'payroll_payment',amount:1050}])).toBe(-850);expect(financeAccountBalance(100,[{transaction_type:'expense',amount:'40'},{transaction_type:'adjustment',amount:'10'},{transaction_type:'income',amount:'25',archived_at:'x'}])).toBe(70)});it('calculates invoice discount and tax',()=>expect(invoiceTotal([{quantity:2,rate:100}],20,10)).toBe(190));it('never returns a negative invoice total',()=>expect(invoiceTotal([{quantity:1,rate:100}],200,0)).toBe(0));it('rejects invalid invoice numbers and rows',()=>{expect(invoiceValidationMessage([{description:'',quantity:1,rate:20}])).toMatch(/description/i);expect(invoiceValidationMessage([{description:'Service',quantity:NaN,rate:20}])).toMatch(/quantity/i);expect(invoiceValidationMessage([{description:'Service',quantity:1,rate:20}],-1,0)).toMatch(/Tax and discount/i);expect(invoiceValidationMessage([{description:'Service',quantity:1,rate:20}])).toBeNull()});it('supports partial and full payments without overpayment',()=>{expect(outstandingInvoice(100,40)).toBe(60);expect(canRecordInvoicePayment(100,40,60)).toBe(true);expect(canRecordInvoicePayment(100,40,61)).toBe(false)});it('calculates payroll net salary',()=>expect(payrollNet(10000,1200,300)).toBe(10900))});
