import {describe,it,expect} from 'vitest';import {classify,dateKey,minutes,monthBounds,monthlyDays} from './attendance-rules';
const s={timezone:'Asia/Kolkata',work_start:'09:00',work_end:'18:00',grace_minutes:10,overtime_after_minutes:480,working_days:[1,2,3,4,5]};
describe('attendance rules',()=>{it('uses IST date keys',()=>expect(dateKey(new Date('2026-01-01T20:00:00Z'),'Asia/Kolkata')).toBe('2026-01-02'));it('detects weekend',()=>expect(classify(new Date('2026-01-03T12:00:00Z'),null,s,new Set())).toBe('weekend'));it('detects holiday',()=>expect(classify(new Date('2026-01-02T12:00:00Z'),null,s,new Set(['2026-01-02']))).toBe('holiday'));it('detects leave',()=>expect(classify(new Date('2026-01-02T12:00:00Z'),null,s,new Set(),true)).toBe('leave'));it('calculates breaks',()=>expect(minutes({clock_in:'2026-01-01T00:00:00Z',clock_out:'2026-01-01T02:00:00Z',break_minutes:30})).toBe(90));it('gets month bounds',()=>expect(monthBounds(new Date('2026-02-10T12:00:00Z'),'Asia/Kolkata')).toEqual({from:'2026-02-01',to:'2026-02-28'}));it('aggregates a month',()=>expect(monthlyDays(new Date('2026-02-10T12:00:00Z'),s,[],new Set(),[]).length).toBe(28))});
