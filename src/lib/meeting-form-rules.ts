import { businessLocalToStored, validateInterval } from './calendar-meeting-rules';
export type MeetingFields={title:string;agenda:string;hostId:string;date:string;start:string;end:string;type:string;venue:string;url:string;notes:string;invitees:string[]};
export function meetingPayload(fields:MeetingFields,target_meeting:string|null=null){if(!fields.title.trim())throw Error('Meeting title is required.');if(!fields.agenda.trim())throw Error('Meeting agenda is required.');if(!fields.hostId)throw Error('Choose an approved meeting host.');const start_at=businessLocalToStored(fields.date,fields.start);const end_at=businessLocalToStored(fields.date,fields.end);validateInterval(start_at,end_at);if(new Date(end_at).getTime()-new Date(start_at).getTime()>12*60*60*1000)throw Error('Meeting duration cannot exceed 12 hours.');if(fields.url&& !/^https?:\/\//.test(fields.url))throw Error('Meeting link must start with http:// or https://.');return {target_meeting,meeting_title:fields.title.trim(),meeting_agenda:fields.agenda.trim(),meeting_start:start_at,meeting_end:end_at,meeting_type_value:fields.type,meeting_venue:fields.venue.trim(),meeting_url_value:fields.url.trim(),meeting_description:fields.notes.trim(),host_profile_id:fields.hostId,participant_ids:[...new Set(fields.invitees)]};}
