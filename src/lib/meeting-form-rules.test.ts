import {describe,it,expect} from 'vitest';import {meetingPayload} from './meeting-form-rules';
const form={title:'Planning',agenda:'Review priorities',hostId:'host-1',date:'2026-08-11',start:'15:00',end:'16:00',type:'office',venue:'Room',url:'',notes:'',invitees:['a','a','b']};
describe('meeting form rules',()=>{it('keeps Kolkata times, host and unique invitees',()=>expect(meetingPayload(form)).toMatchObject({meeting_start:'2026-08-11T09:30:00.000Z',host_profile_id:'host-1',participant_ids:['a','b']}));it('includes the meeting ID and metadata for trusted updates',()=>expect(meetingPayload({...form,title:'Updated',agenda:'Agenda',type:'zoom',venue:'',url:'https://zoom.us/j/1',notes:'Notes'},'meeting-1')).toMatchObject({target_meeting:'meeting-1',meeting_title:'Updated',meeting_agenda:'Agenda',meeting_start:'2026-08-11T09:30:00.000Z',meeting_end:'2026-08-11T10:30:00.000Z',meeting_type_value:'zoom',meeting_url_value:'https://zoom.us/j/1',meeting_description:'Notes'}));it('keeps create mode on the same contract with a null target',()=>expect(meetingPayload(form).target_meeting).toBeNull());it('rejects missing host and agenda',()=>{expect(()=>meetingPayload({...form,hostId:''})).toThrow('approved meeting host');expect(()=>meetingPayload({...form,agenda:''})).toThrow('agenda')});it('rejects invalid intervals and impossible duration',()=>{expect(()=>meetingPayload({...form,end:'15:00'})).toThrow('End time');expect(()=>meetingPayload({...form,start:'01:00',end:'23:30'})).toThrow('12 hours')});it('rejects unsafe meeting links',()=>expect(()=>meetingPayload({...form,url:'javascript:x'})).toThrow('Meeting link'));});
