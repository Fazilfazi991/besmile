export type LocationEvidence={latitude:number;longitude:number;accuracy:number};
export const officeGeofence={latitude:10.0468516,longitude:76.3172664,radiusMetres:100,maxAccuracyMetres:50} as const;
export function haversineMetres(latitude:number,longitude:number,center=officeGeofence){const radians=(value:number)=>value*Math.PI/180;const a=Math.sin(radians(latitude-center.latitude)/2)**2+Math.cos(radians(center.latitude))*Math.cos(radians(latitude))*Math.sin(radians(longitude-center.longitude)/2)**2;return 6371000*2*Math.asin(Math.sqrt(a));}
export function validLocation(location:LocationEvidence){return Number.isFinite(location.latitude)&&Number.isFinite(location.longitude)&&Number.isFinite(location.accuracy)&&location.latitude>=-90&&location.latitude<=90&&location.longitude>=-180&&location.longitude<=180&&location.accuracy>=0&&location.accuracy<=officeGeofence.maxAccuracyMetres;}
export const locationError=(error?:GeolocationPositionError)=>error?.code===1?'Location access is required for attendance. Please enable precise location and try again.':error?.code===3?'Location request timed out. Please try again.':'We could not determine your location. Please enable precise location and try again.';
export function freshLocation():Promise<LocationEvidence>{return new Promise((resolve,reject)=>{if(typeof navigator==='undefined'||!navigator.geolocation)return reject(new Error('Location access is required for attendance. Please use a device or browser with location services enabled.'));navigator.geolocation.getCurrentPosition(position=>resolve({latitude:position.coords.latitude,longitude:position.coords.longitude,accuracy:position.coords.accuracy}),error=>reject(new Error(locationError(error))),{enableHighAccuracy:true,timeout:15000,maximumAge:0});});}
export function formatDistance(metres:number){return metres<1000?`${Math.round(metres)} m`:`${(metres/1000).toFixed(1)} km`;}
