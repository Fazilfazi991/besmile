'use client';

import { FormEvent, useEffect, useMemo, useState } from 'react';
import { currentProfile } from '@/lib/auth';
import { adminRepository } from '@/lib/admin-repository';
import { FinanceEmpty, inr } from '@/components/finance-ui';
import { financeEntryValidationMessage } from '@/lib/finance-master-data-rules';

export const validateFinanceReceipt = (file: File) => !['application/pdf', 'image/jpeg', 'image/png', 'image/webp'].includes(file.type) ? 'Upload a PDF, JPG, PNG, or WebP receipt.' : file.size > 10 * 1024 * 1024 ? 'Receipt must be 10 MB or smaller.' : null;
const date = () => new Date().toISOString().slice(0, 10);

export function FinanceTransactions({ type }: { type: 'income' | 'expense' }) {
  const noun = type === 'income' ? 'Income' : 'Expense';
  const categoryKey = type === 'income' ? 'income_category_id' : 'expense_category_id';
  const [rows, setRows] = useState<any[]>([]); const [options, setOptions] = useState<any>({ accounts: [], income: [], expense: [] }); const [profile, setProfile] = useState<any>(); const [edit, setEdit] = useState<any>(); const [archived, setArchived] = useState(false); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false); const [notice, setNotice] = useState<{ type: 'success' | 'error'; message: string } | null>(null); const [filter, setFilter] = useState({ search: '', account: '', category: '', from: '', to: '' });
  const categories = type === 'income' ? options.income : options.expense;
  const load = async () => { try { setLoading(true); const [p, o, items] = await Promise.all([currentProfile(), adminRepository.financeOptions(), adminRepository.financeTransactions(type, archived)]); setProfile(p); setOptions(o); setRows(items); if (!o.accounts.length || !(type === 'income' ? o.income : o.expense).length) setNotice({ type: 'error', message: !o.accounts.length ? 'No active finance accounts found.' : `No ${noun.toLowerCase()} categories found.` }); } catch (caught: any) { setNotice({ type: 'error', message: caught.message || `Could not load finance options. Try again.` }); } finally { setLoading(false); } };
  useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, [archived]);
  const initial = () => ({ amount: '', transaction_date: date(), account_id: options.accounts[0]?.id || '', [categoryKey]: categories[0]?.id || '', payment_method: 'cash', counterparty_name: '', reference_number: '', description: '', receipt_path: '' });
  const shown = useMemo(() => rows.filter(row => { const term = filter.search.toLowerCase(); const category = row[type === 'income' ? 'income_category' : 'expense_category']?.name || ''; return (!term || [row.counterparty_name, row.reference_number, row.description, category].some(value => String(value || '').toLowerCase().includes(term))) && (!filter.account || row.account_id === filter.account) && (!filter.category || row[categoryKey] === filter.category) && (!filter.from || row.transaction_date >= filter.from) && (!filter.to || row.transaction_date <= filter.to); }), [rows, filter, type, categoryKey]);
  const total = shown.reduce((sum, row) => sum + Number(row.amount || 0), 0); const withReceipt = shown.filter(row => row.receipt_path).length;
  const save = async (event: FormEvent) => { event.preventDefault(); setBusy(true); setNotice(null); try { const validationError = financeEntryValidationMessage({ amount: edit.amount, accountId: edit.account_id, categoryId: edit[categoryKey] }); if (validationError) throw new Error(validationError); let receipt_path = edit.receipt_path || null; if (edit.file) { const fileError = validateFinanceReceipt(edit.file); if (fileError) throw new Error(fileError); receipt_path = await adminRepository.uploadFinanceReceipt(profile.id, edit.file); } const payload = { ...edit, amount: Number(edit.amount), receipt_path, file: undefined }; for (const key of ['id', 'account', 'income_category', 'expense_category', 'archived_at', 'created_at', 'updated_at']) delete payload[key]; if (edit.id) await adminRepository.updateFinanceTransaction(edit.id, payload); else await adminRepository.createFinanceTransaction({ ...payload, transaction_type: type, created_by: profile.id }); setEdit(null); setNotice({ type: 'success', message: `${noun} saved successfully.` }); await load(); } catch (caught: any) { setNotice({ type: 'error', message: caught.message || `Could not save ${noun.toLowerCase()}.` }); } finally { setBusy(false); } };
  const archive = async (row: any) => { if (!window.confirm(row.archived_at ? 'Restore this transaction?' : 'Archive this transaction?')) return; try { if (row.archived_at) await adminRepository.restoreFinanceTransaction(row.id); else await adminRepository.archiveFinanceTransaction(row.id); setNotice({ type: 'success', message: `Transaction ${row.archived_at ? 'restored' : 'archived'}.` }); await load(); } catch (caught: any) { setNotice({ type: 'error', message: caught.message || 'Transaction status could not be updated.' }); } };
  const openReceipt = async (path: string) => { try { window.open(await adminRepository.signedFinanceReceipt(path), '_blank', 'noopener,noreferrer'); } catch (caught: any) { setNotice({ type: 'error', message: caught.message || 'Receipt could not be opened.' }); } };
  return <section className="mx-auto max-w-[1320px] space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">Finance</p><h1 className="text-2xl font-bold">{noun}</h1><p className="mt-1 text-sm text-slate-600">Record, review, and retain receipts for live {noun.toLowerCase()} transactions.</p></div><button className="btn btn-primary" disabled={loading} onClick={() => setEdit(initial())}>Add {noun.toLowerCase()}</button></div><div className="grid gap-3 sm:grid-cols-3"><Metric label={`Filtered ${noun.toLowerCase()}`} value={inr(total)} hint={`${shown.length} transaction${shown.length === 1 ? '' : 's'}`} /><Metric label="Receipts attached" value={String(withReceipt)} hint="Signed private links" /><Metric label="View" value={archived ? 'Archived' : 'Active'} hint={archived ? 'Restorable records' : 'Current ledger entries'} /></div><div className="card grid gap-2 p-3 md:grid-cols-[minmax(210px,1fr)_170px_170px_150px_150px_auto]"><input className="input" placeholder="Search description or reference" value={filter.search} onChange={event => setFilter({ ...filter, search: event.target.value })} /><select className="input" value={filter.account} onChange={event => setFilter({ ...filter, account: event.target.value })}><option value="">All accounts</option>{options.accounts.map((account: any) => <option key={account.id} value={account.id}>{account.name}</option>)}</select><select className="input" value={filter.category} onChange={event => setFilter({ ...filter, category: event.target.value })}><option value="">All categories</option>{categories.map((category: any) => <option key={category.id} value={category.id}>{category.name}</option>)}</select><input className="input" aria-label="From date" type="date" value={filter.from} onChange={event => setFilter({ ...filter, from: event.target.value })} /><input className="input" aria-label="To date" type="date" value={filter.to} onChange={event => setFilter({ ...filter, to: event.target.value })} /><label className="flex items-center gap-2 px-2 text-sm font-medium"><input type="checkbox" checked={archived} onChange={event => setArchived(event.target.checked)} />Archived</label></div>{notice && <p className={`rounded-xl border px-4 py-3 text-sm ${notice.type === 'error' ? 'border-rose-200 bg-rose-50 text-rose-800' : 'border-teal-200 bg-teal-50 text-teal-900'}`}>{notice.message}{notice.type === 'error' && <button className="ml-3 font-semibold underline" onClick={() => void load()}>Retry</button>}</p>}<div className="card overflow-x-auto"><table className="min-w-[1050px] w-full text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{['Date', 'Description / counterparty', 'Category', 'Account', 'Amount', 'Receipt', 'Status', 'Actions'].map(label => <th className="px-4 py-3 text-left" key={label}>{label}</th>)}</tr></thead><tbody>{loading ? Array.from({ length: 4 }, (_, index) => <tr className="border-t" key={index}><td className="px-4 py-5" colSpan={8}><div className="h-4 animate-pulse rounded bg-slate-100" /></td></tr>) : shown.map(row => <tr className="border-t border-slate-100" key={row.id}><td className="px-4 py-3 whitespace-nowrap">{row.transaction_date}</td><td className="px-4 py-3"><b>{row.description || row.counterparty_name || 'Untitled transaction'}</b><small className="block text-slate-500">{row.counterparty_name || row.reference_number || 'No counterparty or reference'}</small></td><td className="px-4 py-3">{row[type === 'income' ? 'income_category' : 'expense_category']?.name || 'Uncategorised'}</td><td className="px-4 py-3">{row.account?.name || '—'}</td><td className="px-4 py-3 font-bold">{inr(row.amount)}</td><td className="px-4 py-3">{row.receipt_path ? <button className="font-medium text-teal-700 hover:underline" onClick={() => void openReceipt(row.receipt_path)}>View receipt</button> : <span className="text-slate-400">Missing</span>}</td><td className="px-4 py-3"><span className={`rounded-full px-2 py-1 text-xs font-bold ${row.archived_at ? 'bg-slate-100 text-slate-600' : 'bg-emerald-50 text-emerald-800'}`}>{row.archived_at ? 'Archived' : 'Active'}</span></td><td className="px-4 py-3 whitespace-nowrap"><button className="font-medium text-teal-700 hover:underline" onClick={() => setEdit({ ...row })}>Edit</button><button className="ml-3 font-medium text-rose-700 hover:underline" onClick={() => void archive(row)}>{row.archived_at ? 'Restore' : 'Archive'}</button></td></tr>)}</tbody></table>{!loading && !shown.length && <FinanceEmpty>No {noun.toLowerCase()} transactions match these filters.</FinanceEmpty>}</div>{edit && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form className="card max-h-[90vh] w-full max-w-2xl overflow-auto p-6" onSubmit={save}><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-bold">{edit.id ? 'Edit' : `Add ${noun.toLowerCase()}`}</h2><p className="mt-1 text-sm text-slate-500">Receipt files: PDF, JPG, PNG, or WebP; maximum 10 MB.</p></div><button type="button" className="text-slate-500 hover:text-slate-950" onClick={() => setEdit(null)}>Close</button></div><div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Amount"><input required min="0.01" step="0.01" className="input" type="number" value={edit.amount} onChange={event => setEdit({ ...edit, amount: event.target.value })} /></Field><Field label="Date"><input required className="input" type="date" value={edit.transaction_date} onChange={event => setEdit({ ...edit, transaction_date: event.target.value })} /></Field><Field label="Account"><select required className="input" value={edit.account_id} onChange={event => setEdit({ ...edit, account_id: event.target.value })}><option value="">Select an account</option>{options.accounts.map((account: any) => <option key={account.id} value={account.id}>{account.name}</option>)}</select></Field><Field label="Category"><select required className="input" value={edit[categoryKey]} onChange={event => setEdit({ ...edit, [categoryKey]: event.target.value })}><option value="">Select a category</option>{categories.map((category: any) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></Field><Field label="Payment method"><select className="input" value={edit.payment_method} onChange={event => setEdit({ ...edit, payment_method: event.target.value })}>{['cash', 'bank_transfer', 'upi', 'card'].map(method => <option key={method}>{method.replaceAll('_', ' ')}</option>)}</select></Field><Field label="Counterparty"><input className="input" value={edit.counterparty_name || ''} onChange={event => setEdit({ ...edit, counterparty_name: event.target.value })} /></Field><Field label="Reference"><input className="input" value={edit.reference_number || ''} onChange={event => setEdit({ ...edit, reference_number: event.target.value })} /></Field><Field label="Receipt"><input className="input" type="file" accept="application/pdf,image/jpeg,image/png,image/webp" onChange={event => setEdit({ ...edit, file: event.target.files?.[0] })} />{edit.file && <small className="mt-1 block text-slate-500">{edit.file.name}</small>}</Field><Field label="Description" className="md:col-span-2"><textarea className="input min-h-24" value={edit.description || ''} onChange={event => setEdit({ ...edit, description: event.target.value })} /></Field></div><div className="mt-6 flex justify-end gap-2"><button type="button" className="btn border" onClick={() => setEdit(null)}>Cancel</button><button disabled={busy || loading || !options.accounts.length || !categories.length} className="btn btn-primary">{busy ? 'Saving…' : 'Save transaction'}</button></div></form></div>}</section>;
}
function Metric({ label, value, hint }: { label: string; value: string; hint: string }) { return <div className="card p-4"><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-xl font-bold">{value}</p><p className="mt-1 text-xs text-slate-500">{hint}</p></div>; }
function Field({ label, children, className = '' }: { label: string; children: React.ReactNode; className?: string }) { return <label className={`block text-sm font-semibold text-slate-700 ${className}`}><span>{label}</span><div className="mt-1.5">{children}</div></label>; }
