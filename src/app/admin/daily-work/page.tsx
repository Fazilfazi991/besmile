"use client";
/* Loading state intentionally resets when the selected review date changes. */
/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect,useMemo,useState } from "react";
import { dateKey } from "@/lib/attendance-rules";
import { employeeRepository } from "@/lib/employee-repository";
const today=()=>dateKey(new Date(),"Asia/Kolkata");
export default function DailyWorkReviewPage(){const[workDate,setWorkDate]=useState(today);const[rows,setRows]=useState<any[]>([]);const[query,setQuery]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{let live=true;setLoading(true);void employeeRepository.dailyWorkUpdates(workDate).then(data=>{if(live){setRows(data);setError("");}}).catch(cause=>{if(live)setError(cause.message||"Daily work updates could not be loaded.");}).finally(()=>{if(live)setLoading(false);});return()=>{live=false;};},[workDate]);const visible=useMemo(()=>rows.filter(row=>`${row.profile?.full_name||""} ${row.profile?.employee_code||""} ${row.summary}`.toLowerCase().includes(query.trim().toLowerCase())),[rows,query]);return <section className="space-y-5"><header><p className="eyebrow">WORK MANAGEMENT</p><h1 className="text-2xl font-bold">Daily Work Updates</h1><p className="text-slate-600">Review updates visible within your workforce access.</p></header><div className="card grid gap-3 p-4 sm:grid-cols-2"><label>Date<input className="input mt-1 w-full" type="date" value={workDate} max={today()} onChange={event=>setWorkDate(event.target.value)}/></label><label>Search<input className="input mt-1 w-full" value={query} onChange={event=>setQuery(event.target.value)} placeholder="Employee or update"/></label></div>{error&&<p className="text-rose-700">{error}</p>}<div className="grid gap-3">{loading?<div className="card p-5 text-slate-500">Loading updates…</div>:visible.map(row=><article className="card p-4" key={row.id}><div className="flex flex-wrap justify-between gap-2"><div><b>{row.profile?.full_name||"Former or unavailable employee"}</b><p className="text-sm text-slate-500">{[row.profile?.employee_code,row.profile?.designation,row.profile?.department?.name].filter(Boolean).join(" · ")||"Employee"}</p></div><time className="text-sm text-slate-500">{new Date(row.updated_at).toLocaleString()}</time></div><p className="mt-3 whitespace-pre-wrap break-words text-sm leading-6 text-slate-700">{row.summary}</p></article>)}{!loading&&!visible.length&&<div className="card p-5 text-slate-500">No work updates match this date and search.</div>}</div></section>}
