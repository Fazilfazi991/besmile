'use client';
import {FormEvent,useEffect,useState} from 'react';
import {adminRepository} from '@/lib/admin-repository';
import {currentProfile} from '@/lib/auth';
export default function AdminDocumentsPage(){const [profile,setProfile]=useState<any>(),[staff,setStaff]=useState<any[]>([]),[documents,setDocuments]=useState<any[]>([]),[requests,setRequests]=useState<any[]>([]),[file,setFile]=useState<File|null>(null),[share,setShare]=useState({title:'',description:'',category:'Policy',profileIds:[] as string[]}),[request,setRequest]=useState({title:'',description:'',due_date:'',profile_id:''}),[comments,setComments]=useState<Record<string,string>>({}),[error,setError]=useState(''),[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);try{const p=await currentProfile() as any;if(!p||!['super_admin','chairman','director','general_manager'].includes(p.role))throw Error('You do not have permission to manage documents.');setProfile(p);const [employees,shared,requested]=await Promise.all([adminRepository.employees('',0,100),adminRepository.documents(),adminRepository.documentRequests()]);setStaff(employees.data);setDocuments(shared);setRequests(requested);setError('')}catch(e:any){setError(e.message)}finally{setLoading(false)}};useEffect(()=>{const timer=setTimeout(()=>void load(),0);return()=>clearTimeout(timer)},[]);const upload=async(e:FormEvent)=>{e.preventDefault();try{if(!file)throw Error('Choose a file to upload.');const stored=await adminRepository.uploadCompanyDocument(profile.id,file);await adminRepository.createCompanyDocument({...share,storage_path:stored.path,file_name:stored.fileName,mime_type:stored.mimeType,file_size:stored.fileSize,uploaded_by:profile.id});setFile(null);setShare({title:'',description:'',category:'Policy',profileIds:[]});await load()}catch(e:any){setError(e.message)}};const createRequest=async(e:FormEvent)=>{e.preventDefault();try{await adminRepository.requestDocument({...request,requested_by:profile.id});setRequest({title:'',description:'',due_date:'',profile_id:''});await load()}catch(e:any){setError(e.message)}};const open=async(path:string)=>window.open(await (await import('@/lib/employee-repository')).employeeRepository.signedDocumentUrl(path),'_blank');if(loading)return <p>Loading documentsâ€¦</p>;if(error&&!profile)return <p className="text-rose-700">{error}</p>;return <section className="space-y-6"><div><h1 className="text-2xl font-bold">Document Management</h1><p className="text-slate-600">Share company documents and review employee submissions.</p></div>{error&&<p className="rounded bg-rose-50 p-3 text-rose-800">{error}</p>}<div className="grid gap-5 lg:grid-cols-2"><form onSubmit={upload} className="card space-y-3 p-5"><h2 className="font-bold">Upload company document</h2><input required className="w-full rounded border p-2" placeholder="Title" value={share.title} onChange={e=>setShare({...share,title:e.target.value})}/><textarea className="w-full rounded border p-2" placeholder="Short description" value={share.description} onChange={e=>setShare({...share,description:e.target.value})}/><select className="w-full rounded border p-2" value={share.category} onChange={e=>setShare({...share,category:e.target.value})}>{['Policy','Form','Notice','HR','Finance','Other'].map(category=><option key={category}>{category}</option>)}</select><select multiple className="w-full rounded border p-2" value={share.profileIds} onChange={e=>setShare({...share,profileIds:Array.from(e.target.selectedOptions,o=>o.value)})}>{staff.map(employee=><option value={employee.id} key={employee.id}>{employee.full_name}</option>)}</select><p className="text-xs text-slate-500">Leave staff selection empty to share with everyone.</p><input required type="file" onChange={e=>setFile(e.target.files?.[0]||null)}/><button className="rounded bg-slate-900 px-4 py-2 text-white">Upload and share</button></form><form onSubmit={createRequest} className="card space-y-3 p-5"><h2 className="font-bold">Request a document</h2><input required className="w-full rounded border p-2" placeholder="Requested document" value={request.title} onChange={e=>setRequest({...request,title:e.target.value})}/><textarea className="w-full rounded border p-2" placeholder="Short instructions" value={request.description} onChange={e=>setRequest({...request,description:e.target.value})}/><input type="date" className="w-full rounded border p-2" value={request.due_date} onChange={e=>setRequest({...request,due_date:e.target.value})}/><select required className="w-full rounded border p-2" value={request.profile_id} onChange={e=>setRequest({...request,profile_id:e.target.value})}><option value="">Select employee</option>{staff.map(employee=><option value={employee.id} key={employee.id}>{employee.full_name}</option>)}</select><button className="rounded bg-slate-900 px-4 py-2 text-white">Send request</button></form></div><div className="card overflow-hidden"><h2 className="border-b p-5 font-bold">Company documents</h2>{documents.map(document=><div className="flex justify-between border-b p-4" key={document.id}><span><b>{document.title}</b><small className="ml-2 text-slate-500">{document.category}</small><p className="text-sm text-slate-600">{document.description}</p></span><button className="text-sm underline" onClick={()=>open(document.storage_path)}>Preview / download</button></div>)}</div><div className="card overflow-hidden"><h2 className="border-b p-5 font-bold">Employee submissions</h2>{requests.map(item=><article className="border-b p-4" key={item.id}><div className="flex flex-wrap justify-between gap-3"><div><b>{item.title}</b><p className="text-sm text-slate-600">{item.employee?.full_name} Â· {item.status}</p>{item.document_submissions?.[0]&&<button className="mt-2 text-sm underline" onClick={()=>open(item.document_submissions[0].storage_path)}>Preview / download submission</button>}</div>{item.status==='submitted'&&<div className="space-y-2"><input className="rounded border p-2 text-sm" placeholder="Optional review comment" value={comments[item.id]||''} onChange={e=>setComments({...comments,[item.id]:e.target.value})}/><div className="flex gap-2"><button onClick={async()=>{await adminRepository.reviewDocumentRequest(item.id,'approved',comments[item.id]||'',profile.id);await load()}} className="rounded border px-3 py-2 text-sm">Approve</button><button onClick={async()=>{await adminRepository.reviewDocumentRequest(item.id,'rejected',comments[item.id]||'',profile.id);await load()}} className="rounded border border-rose-300 px-3 py-2 text-sm text-rose-700">Reject</button></div></div>}</div></article>)}{!requests.length&&<p className="p-5 text-slate-600">No document requests yet.</p>}</div></section>}
