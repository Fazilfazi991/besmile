'use client';
import {useEffect,useState} from 'react';import {adminRepository} from '@/lib/admin-repository';import {currentProfile} from '@/lib/auth';import {employeeRepository} from '@/lib/employee-repository';
export default function AdminNotificationsPage(){const [profile,setProfile]=useState<any>(),[items,setItems]=useState<any[]>([]),[error,setError]=useState(''),[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);try{const p=await currentProfile() as any;if(!p||!['super_admin','chairman','director','general_manager'].includes(p.role))throw Error('You do not have permission to view organization notifications.');setProfile(p);setItems(await adminRepository.notifications());setError('')}catch(e:any){setError(e.message)}finally{setLoading(false)}};useEffect(()=>{const timer=setTimeout(()=>void load(),0);return()=>clearTimeout(timer)},[]);if(loading)return <p>Loading notifications…</p>;if(error)return <p className="text-rose-700">{error}</p>;return <section className="space-y-5"><div className="flex justify-between"><div><h1 className="text-2xl font-bold">Organization Notifications</h1><p className="text-slate-600">Leave, documents, tasks, and system activity.</p></div><button className="rounded border px-3 py-2 text-sm" onClick={async()=>{await employeeRepository.markAllNotificationsRead(profile.id);await load()}}>Mark my notifications read</button></div><div className="card overflow-hidden">{items.map(item=><article className={`border-b p-4 ${item.read_at?'':'bg-sky-50'}`} key={item.id}><b>{item.title}</b><p className="text-sm text-slate-600">{item.body}</p><small className="text-slate-500">To: {item.recipient?.full_name||'Employee'} · {new Date(item.created_at).toLocaleString()}</small>{item.deep_link&&<a className="mt-2 inline-block text-sm underline" href={item.deep_link}>Open related item</a>}</article>)}{!items.length&&<p className="p-5 text-slate-600">No organization notifications.</p>}</div></section>}
