import type { Metadata, Viewport } from 'next'; import './globals.css'; import './standard-attendance.css'; import './layout-stability.css'; import './print.css'; export const metadata:Metadata={title:'BSmile CRM',description:'BSmile internal portal',manifest:'/manifest.webmanifest',icons:{icon:'/icons/bsmile-icon.svg'}}; export const viewport:Viewport={themeColor:'#0f766e'}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
